# Authentication

The public website scanner does not currently expose a protected public API requiring agent authentication.

For implementation requests, contact hello@aiwebscan.nl. Do not send secrets or personal data in URL parameters.
